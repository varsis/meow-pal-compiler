{ 2 lexical errors:
	line 57: string spans newline
	line 82: unterminated string
}


program example(input, output);
type	
	inner	= array [1..3] of real;
	mat	= array [1..3] of inner;

var
	a	: integer;
	b	: integer;
	norm	: real;
	M	: mat;
	d	: integer;


{ multi-line comment
}

function matrixnorm(A : mat) : real;
var
	i	: integer;
	j	: integer;
	max	: real;
	sum	: real;
begin

	max := 0.0;
	j := 1;
	while j <= 3 do
	begin
		sum := 0.0;
		i := 1;
		while i <= 3 do
		begin
			sum := sum + A[i][j];
			i := i + 1;
		end;
		if max < abs(sum) then max := abs(sum);
		j := j + 1; 
	end;

	matrixnorm := max;
end;


begin

	writeln('This is a \'\'multiline string!
We can\'t \'\'allow these!');

	// Initialize the M matrix to something
	a := 1;
	while a <= 3 do
	begin

		b := 0;
		while b <= 3 do
		begin

			if ((a + b) mod 2) = 0 then
			begin			  
				M[a][b] := a * b * 1.333;
			end
			else
			begin
				M[a][b] := a * b * 2.778;
			end;
			b := b + 1;
		end;

		a := a + 1;
	end;

	writeln('This is a non-terminating \'\'string!


	// Find the matrix norm
	norm := matrixnorm(M);

end.
