program testProcDecls(input, output);

procedure dostuff(a : integer);
begin
	writeln('stuff');
end;

function dostuff2(a : integer; b : integer) : string;
begin
	writeln('stuff');
	dostuff2 := 'word';
end;

begin
	dostuff(1);
end.
