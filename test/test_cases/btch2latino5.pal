{ 5.pal contains semantic errors only }
{ This program was originally the exercise 2, developed by Bob Beck }
{ Error 1: line: 37 - Semantic: 'n' not defined}
{ Error 2: line: 37 - Semantic: 'oddcount' not defined}
{ Error 3: line: 37 - Semantic: 'evencount' not defined}

program errors(input, output);


	procedure ODO ( x : integer );
        var n,oddcount, evencount : integer;
            no : char;
		procedure EVEN;
		begin
			if x <> 0 then begin
				evencount := evencount +1;
				x := x div 2;
                                no = 'a';
				if odd(x) then ODO ( x )
    				else EVEN
			end;
		end;
	begin
		oddcount := oddcount + 1;
		x := x div 2;
		if odd(x) then ODO ( x )
    		else EVEN
	end;

begin 
read ( n );
if n >= 0 then begin
	if odd(n) then ODO ( n )
        {Semantic: 'n' not defined}
        {Semantic: 'oddcount' not defined}
        {Semantic: 'evencount' not defined}
	writeln('n = ',n,'  o = ',oddcount,'  e = ',evencount);

end;
end.
