program test013(input, output);

type
        enum_month = (Jan, Feb, Mar, Apr, May, Jun, Jul, Aug, Sep,
        Oct, Nov, Dec);

var
        month : enum_month;

begin
        month := Mar;
        writeln(ord(month));
end.
