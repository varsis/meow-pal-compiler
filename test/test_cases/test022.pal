program test022(input, output);

var
        weird_array : array[Boolean] of integer;

begin
        weird_array[true] := 1;
        weird_array[false] := 2;
        writeln(weird_array[true], '  ', weird_array[false]);
end.
