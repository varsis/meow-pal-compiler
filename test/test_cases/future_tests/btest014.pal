program btest014(input, output);

{ Test Case: exit outside of a while loop. Probably just handle during 
  semantic analysis, too tricky while parsing. }

begin
        writeln('This is a program.');
        readln();
        exit;
end.
