program test019(input, output);

var
        speed : real;
        age   : integer;
        for_sale : Boolean;

begin
        speed := (23+34*2*(20/10));
        age   := 23.233;
        for_sale := false;
        writeln(speed, '   ', age, '   ', for_sale);
end.
