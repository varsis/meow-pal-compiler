program btest013(input, output);

{ Test Case: continue outside of a while loop. Probably just handle during 
  semantic analysis, too tricky while parsing. }

var
        str : string;
begin
        str := 'hello';
        continue;
        write(str);
end.
