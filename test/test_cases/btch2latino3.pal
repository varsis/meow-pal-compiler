{ 3.pal contains semantic errors only }
{ This program was originally the exercise 2, developed by Bob Beck }
{ Error 1: line: 33 - Semantic: variable not defined}

program main(input, output);
	label 1;
	var n,oddcount, evencount : integer;
            no : char;

	procedure ODO ( x : integer );
		procedure EVEN;
		begin
			if x <> 0 then begin
				evencount := evencount +1;
				x := x div 2;
				if odd(x) then ODO ( x )
    				else EVEN
			end;
		end;
	begin
		oddcount := oddcount + 1;
		x := x div 2;
		if odd(x) then ODO ( x )
    		else EVEN
	end;

begin 
1: read ( n );
if n >= 0 then begin
	oddcount := 0;
	evencount := 0;
	if odd(n) then ODO ( n )
    	else EVEN ( n ); {Semantic: variable not defined}
	writeln('n = ',n,'  o = ',oddcount,'  e = ',evencount);
	goto 1;
end;
end.
