//################################
//# Project PAL		         #
//# Course: CMPUT 415 A1         #
//# Prof: Piotr Rudnicki         #
//# Group: c415g09               #
// this our assignment 2 PASCAL program modified so there are some illegal things:
// 1) label declaration line 16, discard declaration to recover
// 2) illegal character line 17, should recover
// 3) procedure declaration without parameter line 20, hard to recover
// 4) label call line 53, discard to recover
// 5) illegal goto call line 60, discard to recover
// 


program main(input, output);
	label 1;
	var int&: pointer;
	var n,a, b, oddcount, evencount : integer;
	procedure ODD ( x : integer );
	procedure EVEN;

	begin
		if x <> 0 then begin
			x := x div 2;
			if odd(x) then ODD ( x )
			else EVEN
		end;
	end;
begin
	oddcount := oddcount + 1;
	x := x div 2;
	if odd(x) then ODD ( x )
	else EVEN
	end;

procedure EVEN ( x : integer);
	procedure ODD ( var x : integer );
	begin
		oddcount := oddcount + 1;
		x := x div 2;
		if odd(x) then ODD ( X )
		else EVEN ( x )
	end;
begin
	if x <> 0 then begin
		evencount := evencount + 1;
		x := x div 2;
		if odd(x) then ODD ( x )
		else EVEN ( x )
	end;
end;
begin
1: read ( n );
	if n >= 0 then begin
	oddcount := 0;
	evencount := 0;
	if odd(n) then ODD ( n )
	else EVEN ( n );
	writeln('n =', n, 'o =', oddcount, 'e =', evencount );
	goto 1;
end;
	snafblatz := 1.0;
end.
