{ 4.pal contains semantic errors only }
{ This program was originally the exercise 2, developed by Bob Beck }
{ Error 1: line: 9 - Semantic error: redefinition of 'n'}
{ Error 2: line: 48 - Semantic error: variable not defined }

program main(input, output);
	label 1;
	var n,oddcount, evencount : integer;
            n : char; {semantic error: redefinition}

	procedure ODO ( x : integer );
		procedure EVEN;
		begin
			if x <> 0 then begin
				evencount := evencount +1;
				x := x div 2;
				if odd(x) then ODO ( x )
    				else EVEN
			end;
		end;
	begin
		oddcount := oddcount + 1;
		x := x div 2;
		if odd(x) then ODO ( x )
    		else EVEN
	end;

	procedure EVEN ( x : integer);
		procedure ODO(var x : integer);
		begin
			oddcount := oddcount + 1;
			x := x div 2;
			if odd(x) then ODO ( x )
    			else EVEN ( x )
		end;
	begin
		if x <> 0 then begin
			evencount := evencount + 1;
			x := x div 2;
			if odd(x) then ODO ( x )
    			else EVEN ( X )
		end;
	end;

begin 
1: read ( n );
if n >= 0 then begin
	oddcunt := 0; {Semantic error: variable not defined}
	evencount := 0;
	if odd(n) then ODO ( n )
    	else EVEN ( n );
	writeln('n = ',n,'  o = ',oddcount,'  e = ',evencount);
	goto 1;
end;
end.
