################################################################################
# pow(x, n) - find x^n (Internal Only)
################################################################################

# Test
#	READR
#	CALL	0, ml_pow
#	!D
#	STOP

ml_pow
	# exp(n * ln(x))
	PUSH	-4[0]
	PUSH	-3[0]
	CALL	0, ml_ln
	MULR
	CALL	0, ml_exp

	# retvalue = exp(n * ln(x))
	POP	-3[0]
	RET	0
	
